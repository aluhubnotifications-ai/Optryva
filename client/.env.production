# Split deploy: this Pages site (optryva.pages.dev) calls the API Worker over
# HTTPS (cross-origin). Backend Worker is named "optryva-api".
VITE_API_URL=https://optryva-api.aluhub-notifications.workers.dev/api
