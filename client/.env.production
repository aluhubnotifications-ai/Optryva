# Split deploy: this Pages site calls the API Worker over HTTPS (cross-origin).
# Backend Worker is named "optryva" → optryva.aluhub-notifications.workers.dev.
VITE_API_URL=https://optryva.aluhub-notifications.workers.dev/api
